# Preserved attempt 01

This is the first baseline invocation for `run-01`. It was executed before the
candidate source existed and therefore recorded `non-executable`. It is kept
unchanged as protocol evidence; the later executable baseline is stored in the
canonical `results/baseline/run-01/` directory.
