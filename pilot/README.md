# Pilot artifacts

This directory contains non-research fixtures used to rehearse the evaluation
pipeline and report layout. Pilot rows must never be merged into the canonical
`experiments/metadata.csv` or `results/measurements.csv`.

The checked-in snapshot under `synthetic-evaluation/` was generated without an
external AI session. See its README and marker before using any artifact.
