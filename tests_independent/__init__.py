"""Independent, human-oracle-owned verification suite.

The suite is intentionally separate from ``tests/`` and from each candidate's
``tests_ai`` directory.  The runner puts one candidate source tree on
``PYTHONPATH`` at a time.
"""
